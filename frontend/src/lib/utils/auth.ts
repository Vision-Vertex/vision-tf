// Auth Utilities - Hour 4.2 (20 min)
// - isTokenExpired(token) - Check if JWT token is expired
// - decodeToken(token) - Decode JWT token payload
// - getTokenExpiration(token) - Get token expiration time
// - isRememberMeEnabled() - Check if remember me is enabled
// - clearAuthData() - Clear all auth data from storage
// - validatePassword(password) - Validate password strength
// - validateEmail(email) - Validate email format
